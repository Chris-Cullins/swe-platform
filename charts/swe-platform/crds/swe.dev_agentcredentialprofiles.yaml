---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: agentcredentialprofiles.swe.dev
spec:
  group: swe.dev
  names:
    kind: AgentCredentialProfile
    listKind: AgentCredentialProfileList
    plural: agentcredentialprofiles
    singular: agentcredentialprofile
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          AgentCredentialProfile declares metadata for an agent credential. Secret material is
          stored separately and is never represented in this API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AgentCredentialProfileSpec describes a purpose-scoped agent
              credential.
            properties:
              adapter:
                description: Adapter is the agent adapter allowed to consume this
                  credential.
                maxLength: 63
                minLength: 1
                type: string
              credentialType:
                description: CredentialType selects the credential format.
                enum:
                - APIKey
                type: string
            required:
            - adapter
            - credentialType
            type: object
            x-kubernetes-validations:
            - message: spec is immutable
              rule: self == oldSelf
        required:
        - spec
        type: object
    served: true
    storage: true
